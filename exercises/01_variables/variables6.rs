// variables6.rs
//
// Execute `rustlings hint variables6` or use the `hint` watch subcommand for a
// hint.

///Here we see that a const type is immutable by default, usable in the whole scope, but neetd to always be typed
const NUMBER:i32 = 3;
fn main() {
    println!("Number {}", NUMBER);
}
